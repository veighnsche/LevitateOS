[package]
name = "recstrap"
version = "0.1.0"
edition = "2021"
description = "LevitateOS installer - extracts squashfs to disk"
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.4", features = ["derive"] }

[profile.release]
strip = true
lto = true
