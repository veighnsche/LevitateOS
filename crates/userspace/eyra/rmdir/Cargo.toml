# TEAM_364: uutils-based rmdir for LevitateOS
[package]
name = "rmdir"
version = "0.1.0"
edition = "2021"

[dependencies]
eyra = { version = "0.22", features = ["experimental-relocate"] }
uu_rmdir = "0.2"

# TEAM_367: Fixed - added build.rs with -nostartfiles
