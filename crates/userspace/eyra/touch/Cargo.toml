# TEAM_364: uutils-based touch for LevitateOS
[package]
name = "touch"
version = "0.1.0"
edition = "2021"

[dependencies]
eyra = { version = "0.22", features = ["experimental-relocate"] }
uu_touch = "0.2"

