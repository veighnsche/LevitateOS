# TEAM_378: Prevent per-utility stale artifacts
# Only workspace-level target/, Cargo.lock, and .cargo/ are authoritative

# Ignore per-utility target directories (workspace uses eyra/target/)
/*/target/

# Ignore per-utility Cargo.lock (workspace uses eyra/Cargo.lock)
/*/Cargo.lock

# Ignore per-utility .cargo folders (workspace uses eyra/.cargo/)
/*/.cargo/
