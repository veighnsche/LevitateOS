# TEAM_364: uutils-based pwd for LevitateOS
[package]
name = "pwd"
version = "0.2.0"
edition = "2021"
description = "GNU-compatible pwd from uutils-coreutils"

[dependencies]
eyra = { version = "0.22", features = ["experimental-relocate"] }
uu_pwd = "0.2"

