# TEAM_364: uutils-based env for LevitateOS
[package]
name = "env"
version = "0.1.0"
edition = "2021"

[dependencies]
eyra = { version = "0.22", features = ["experimental-relocate"] }
uu_env = "0.2"

