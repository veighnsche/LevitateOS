[package]
name = "libsyscall"
version = "0.1.0"
edition = "2021"
description = "Userspace syscall library for LevitateOS"

[lints]
workspace = true

[lib]
[dependencies]
linux-raw-sys = { version = "0.4", default-features = false, features = ["errno", "general", "ioctl", "no_std"] }
