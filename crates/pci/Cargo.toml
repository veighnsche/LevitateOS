[package]
name = "los_pci"
version = "0.1.0"
edition = "2024"
readme = "README.md"

[lints]
workspace = true

[features]
default = []
std = ["los_hal/std"]

[dependencies]
los_hal = { path = "../hal" }
# TEAM_141: Use workspace dependencies for version consistency
virtio-drivers.workspace = true
