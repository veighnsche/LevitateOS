# Acorn Variant (Stage 00 Scaffold)

Required Stage 00 files:
- `kconfig`
- `00Build.toml`
- `recipes/kernel.rhai`
- `00Build-build-capability.sh`

`00Build.toml` is the Stage 00 source of truth for Acorn.
